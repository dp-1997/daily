{
  "name": "the-dj",
  "private": true,
  "type": "module",
  "description": "The DJ, Damian Pickett's morning paper. Zero dependencies.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "fetch": "node scripts/fetch.mjs",
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check",
    "serve": "npx serve -l 3456 ."
  }
}
